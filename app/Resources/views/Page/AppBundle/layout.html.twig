{# app/Resources/views/page/appBundle/layout.html.twig #}
{%  extends '::base.html.twig' %}

{% block navigation %}
    coucou je suis la navigation
{% endblock %}

{%  block body%}

    {% for type, messages in app.session.flashBag.all %}
        {% for message in messages %}
            <div class="{{ type }}">
                {{ message|trans({}, 'FOSUserBundle') }}
            </div>
        {% endfor %}
    {% endfor %}

    <div>
        Ici il y aura pleins d'url pour le panneau de controle
    </div>


{%  endblock %}

{% block footer %}
    Et moi je suis le footer niak niak
    Symfony2 Panneau de controle - created by Nicolas LAMBS
{% endblock %}